---
title: 安装指南
description: Antigravity Agent 详细安装步骤和系统要求
category: getting-started
language: zh-CN
version: 1.0.3
lastUpdated: 2025-12-04
tags: [安装, 系统要求, Windows, macOS, Linux]
---

# 安装指南

本文档提供 Antigravity Agent 在不同操作系统上的详细安装步骤、系统要求和常见问题解决方案。

## 目录

- [系统要求](#系统要求)
- [Windows 安装](#windows-安装)
- [macOS 安装](#macos-安装)
- [Linux 安装](#linux-安装)
- [验证安装](#验证安装)
- [常见安装问题](#常见安装问题)
- [卸载指南](#卸载指南)

## 系统要求

### 通用要求

- **磁盘空间**：至少 200MB 可用空间
- **内存**：至少 4GB RAM（推荐 8GB）
- **网络**：需要互联网连接以下载安装包和使用 Antigravity 服务

### Windows 系统要求

- **操作系统**：Windows 10 (64-bit) 或更高版本
- **架构**：x64 (64-bit)
- **运行时**：无需额外安装运行时环境
- **权限**：需要管理员权限进行安装

### macOS 系统要求

- **操作系统**：macOS 10.15 (Catalina) 或更高版本
- **架构**：
  - Intel (x86_64)
  - Apple Silicon (ARM64 - M1/M2/M3)
- **权限**：需要允许来自未识别开发者的应用（首次运行）

### Linux 系统要求

> ⚠️ **注意**：Linux 支持目前处于计划阶段，尚未正式发布。

预计支持的发行版：
- Ubuntu 20.04 LTS 或更高版本
- Fedora 35 或更高版本
- Arch Linux（滚动更新）
- Debian 11 或更高版本


## Windows 安装

### 下载安装包

1. 访问 [Releases 页面](https://github.com/MonchiLin/antigravity-agent/releases)
2. 找到最新版本（例如：v1.0.3）
3. 下载 Windows 安装包：
   - `Antigravity-Agent_x.x.x_x64_en-US.msi` - MSI 安装包（推荐）
   - `Antigravity-Agent_x.x.x_x64-setup.exe` - NSIS 安装程序

### 安装步骤（MSI 安装包）

1. **运行安装程序**
   - 双击下载的 `.msi` 文件
   - 如果出现 Windows SmartScreen 警告，点击"更多信息"，然后点击"仍要运行"

2. **用户账户控制（UAC）提示**
   - 点击"是"以允许安装程序进行更改

3. **安装向导**
   - 点击"Next"开始安装
   - 选择安装位置（默认：`C:\Program Files\Antigravity Agent`）
   - 点击"Install"开始安装

4. **完成安装**
   - 安装完成后，勾选"Launch Antigravity Agent"
   - 点击"Finish"完成安装并启动应用

### 安装步骤（NSIS 安装程序）

1. **运行安装程序**
   - 双击下载的 `-setup.exe` 文件
   - 处理 SmartScreen 警告（如上）

2. **选择安装选项**
   - 选择安装位置
   - 选择是否创建桌面快捷方式
   - 选择是否添加到开始菜单

3. **完成安装**
   - 点击"Install"
   - 等待安装完成
   - 点击"Finish"

### Windows 安装后配置

安装完成后，应用程序会在以下位置创建配置文件：

- **配置目录**：`%APPDATA%\.antigravity-agent\`
- **日志目录**：`%APPDATA%\.antigravity-agent\logs\`
- **数据库文件**：`%APPDATA%\.antigravity-agent\antigravity-agent.db`

实际路径示例：`C:\Users\YourUsername\AppData\Roaming\.antigravity-agent\`

### Windows 开机自启动（可选）

1. 按 `Win + R` 打开运行对话框
2. 输入 `shell:startup` 并按回车
3. 在打开的文件夹中创建 Antigravity Agent 的快捷方式
4. 或者在应用程序设置中启用"开机自启动"选项


## macOS 安装

### 下载安装包

1. 访问 [Releases 页面](https://github.com/MonchiLin/antigravity-agent/releases)
2. 找到最新版本（例如：v1.0.3）
3. 根据您的 Mac 架构下载对应的安装包：
   - **Intel Mac**：`Antigravity-Agent_x.x.x_x64.dmg`
   - **Apple Silicon (M1/M2/M3)**：`Antigravity-Agent_x.x.x_aarch64.dmg`
   - **通用版本**：`Antigravity-Agent_x.x.x_universal.dmg`（同时支持 Intel 和 Apple Silicon）

> 💡 **提示**：如果不确定您的 Mac 架构，点击左上角的 Apple 图标 > "关于本机"，查看"芯片"或"处理器"信息。

### 安装步骤

1. **打开 DMG 文件**
   - 双击下载的 `.dmg` 文件
   - 等待磁盘映像挂载

2. **安装应用程序**
   - 在打开的窗口中，将 `Antigravity Agent.app` 拖动到 `Applications` 文件夹
   - 等待复制完成

3. **首次启动**
   - 打开 `Applications` 文件夹
   - 双击 `Antigravity Agent` 启动应用

4. **处理安全提示**
   
   如果出现"无法打开，因为它来自身份不明的开发者"提示：
   
   **方法一：通过系统偏好设置允许**
   - 打开"系统偏好设置"（或"系统设置"）
   - 进入"安全性与隐私"（或"隐私与安全性"）
   - 在"通用"标签页中，点击"仍要打开"按钮
   - 在确认对话框中点击"打开"

   **方法二：通过右键菜单打开**
   - 在 Finder 中找到 Antigravity Agent
   - 按住 `Control` 键并点击应用图标（或右键点击）
   - 选择"打开"
   - 在弹出的对话框中点击"打开"

5. **授予权限**
   
   首次运行时，可能需要授予以下权限：
   - **辅助功能权限**：用于管理 Antigravity 进程
   - **文件访问权限**：用于读写配置文件
   
   根据系统提示，在"系统偏好设置"中授予相应权限。

### macOS 安装后配置

安装完成后，应用程序会在以下位置创建配置文件：

- **配置目录**：`~/.config/.antigravity-agent/`
- **日志目录**：`~/.config/.antigravity-agent/logs/`
- **数据库文件**：`~/.config/.antigravity-agent/antigravity-agent.db`

实际路径示例：`/Users/YourUsername/.config/.antigravity-agent/`

### macOS 开机自启动（可选）

1. 打开"系统偏好设置"（或"系统设置"）
2. 进入"用户与群组"（或"通用" > "登录项"）
3. 点击"登录项"标签页
4. 点击"+"按钮
5. 在 Applications 文件夹中选择 Antigravity Agent
6. 点击"添加"

或者在应用程序设置中启用"开机自启动"选项。

### 卸载 DMG 文件

安装完成后，可以安全地卸载 DMG 磁盘映像：
1. 在 Finder 侧边栏中找到挂载的磁盘映像
2. 点击旁边的"推出"按钮
3. 删除下载的 `.dmg` 文件（可选）


## Linux 安装

> 🚧 **开发中**：Linux 版本目前处于计划阶段，尚未正式发布。以下是预期的安装方式。

### 预期支持的安装包格式

- **AppImage**：通用格式，无需安装，直接运行
- **DEB**：适用于 Debian/Ubuntu 系列
- **RPM**：适用于 Fedora/RHEL 系列
- **Snap**：跨发行版的通用包管理格式
- **Flatpak**：另一种跨发行版的包管理格式

### 预期安装步骤（AppImage）

```bash
# 下载 AppImage 文件
wget https://github.com/MonchiLin/antigravity-agent/releases/download/vX.X.X/Antigravity-Agent_X.X.X_amd64.AppImage

# 添加执行权限
chmod +x Antigravity-Agent_X.X.X_amd64.AppImage

# 运行应用
./Antigravity-Agent_X.X.X_amd64.AppImage
```

### 预期安装步骤（DEB - Debian/Ubuntu）

```bash
# 下载 DEB 包
wget https://github.com/MonchiLin/antigravity-agent/releases/download/vX.X.X/antigravity-agent_X.X.X_amd64.deb

# 安装
sudo dpkg -i antigravity-agent_X.X.X_amd64.deb

# 如果有依赖问题，运行
sudo apt-get install -f
```

### 预期安装步骤（RPM - Fedora/RHEL）

```bash
# 下载 RPM 包
wget https://github.com/MonchiLin/antigravity-agent/releases/download/vX.X.X/antigravity-agent-X.X.X.x86_64.rpm

# 安装
sudo rpm -i antigravity-agent-X.X.X.x86_64.rpm

# 或使用 dnf
sudo dnf install antigravity-agent-X.X.X.x86_64.rpm
```

### Linux 配置目录（预期）

- **配置目录**：`~/.config/.antigravity-agent/`
- **日志目录**：`~/.config/.antigravity-agent/logs/`
- **数据库文件**：`~/.config/.antigravity-agent/antigravity-agent.db`

### 关注开发进度

如果您希望在 Linux 上使用 Antigravity Agent，请：
- 在 [GitHub Issues](https://github.com/MonchiLin/antigravity-agent/issues) 中表达您的需求
- 关注项目的 [Releases 页面](https://github.com/MonchiLin/antigravity-agent/releases)
- 参与 [讨论区](https://github.com/MonchiLin/antigravity-agent/discussions)


## 验证安装

安装完成后，您可以通过以下方式验证安装是否成功：

### 1. 启动应用程序

- **Windows**：从开始菜单或桌面快捷方式启动
- **macOS**：从 Applications 文件夹或 Launchpad 启动
- **Linux**：从应用程序菜单或命令行启动

### 2. 检查版本信息

启动应用后，可以在以下位置查看版本信息：
- 应用程序窗口标题栏
- 设置 > 关于页面
- 系统托盘图标右键菜单 > 关于

### 3. 验证配置文件

检查配置目录是否已创建：

**Windows**：
```powershell
dir %APPDATA%\.antigravity-agent
```

**macOS/Linux**：
```bash
ls -la ~/.config/.antigravity-agent
```

应该能看到以下文件和目录：
- `antigravity-agent.db` - 数据库文件
- `logs/` - 日志目录
- `config.json` - 配置文件（首次运行后创建）

### 4. 测试基本功能

1. 启动 Antigravity Agent
2. 应用应该能够自动检测当前的 Antigravity 安装
3. 如果已登录 Antigravity，应该能看到当前用户信息

如果以上步骤都正常，说明安装成功！


## 常见安装问题

### Windows 常见问题

#### 问题 1：SmartScreen 阻止安装

**症状**：Windows 显示"Windows 已保护你的电脑"警告

**解决方案**：
1. 点击"更多信息"
2. 点击"仍要运行"
3. 这是因为应用尚未获得 Microsoft 的代码签名证书，属于正常现象

#### 问题 2：安装需要管理员权限

**症状**：安装时提示需要管理员权限

**解决方案**：
1. 右键点击安装程序
2. 选择"以管理员身份运行"
3. 在 UAC 提示中点击"是"

#### 问题 3：安装后无法启动

**症状**：双击图标后应用无响应或立即关闭

**解决方案**：
1. 检查是否安装了最新的 Windows 更新
2. 检查防病毒软件是否阻止了应用
3. 尝试以管理员身份运行
4. 查看日志文件：`%APPDATA%\.antigravity-agent\logs\`

#### 问题 4：防病毒软件误报

**症状**：防病毒软件将应用标记为威胁

**解决方案**：
1. 这是误报，因为应用使用了 Tauri 框架
2. 将应用添加到防病毒软件的白名单
3. 或暂时禁用防病毒软件进行安装

### macOS 常见问题

#### 问题 1：无法打开，因为来自身份不明的开发者

**症状**：双击应用时显示安全警告

**解决方案**：
1. 打开"系统偏好设置" > "安全性与隐私"
2. 在"通用"标签页中，点击"仍要打开"
3. 或者右键点击应用，选择"打开"

#### 问题 2：应用已损坏，无法打开

**症状**：提示"Antigravity Agent 已损坏，无法打开"

**解决方案**：
这通常是因为 macOS 的 Gatekeeper 检查。运行以下命令：

```bash
# 移除隔离属性
xattr -cr /Applications/Antigravity\ Agent.app

# 或者禁用 Gatekeeper（不推荐）
sudo spctl --master-disable
```

#### 问题 3：Apple Silicon (M1/M2/M3) 兼容性问题

**症状**：应用运行缓慢或崩溃

**解决方案**：
1. 确保下载了 ARM64 或 Universal 版本
2. 不要使用 Intel 版本（会通过 Rosetta 2 运行，性能较差）
3. 检查"活动监视器"中应用的架构类型

#### 问题 4：权限问题

**症状**：应用无法访问文件或执行某些操作

**解决方案**：
1. 打开"系统偏好设置" > "安全性与隐私" > "隐私"
2. 检查以下权限：
   - 完全磁盘访问权限
   - 辅助功能权限
3. 将 Antigravity Agent 添加到允许列表

### Linux 常见问题（预期）

#### 问题 1：AppImage 无法运行

**解决方案**：
```bash
# 确保有执行权限
chmod +x Antigravity-Agent_X.X.X_amd64.AppImage

# 安装 FUSE（如果需要）
sudo apt install fuse libfuse2  # Debian/Ubuntu
sudo dnf install fuse fuse-libs  # Fedora
```

#### 问题 2：依赖缺失

**解决方案**：
```bash
# Ubuntu/Debian
sudo apt-get install -f

# Fedora
sudo dnf install --skip-broken
```

### 通用问题

#### 问题：配置文件损坏

**症状**：应用启动后行为异常或崩溃

**解决方案**：
1. 备份配置目录
2. 删除配置文件，让应用重新创建：
   - Windows: `%APPDATA%\.antigravity-agent\`
   - macOS/Linux: `~/.config/.antigravity-agent/`
3. 重新启动应用

#### 问题：端口冲突

**症状**：应用提示端口被占用

**解决方案**：
1. 检查是否有其他 Antigravity 相关进程在运行
2. 使用任务管理器（Windows）或活动监视器（macOS）结束相关进程
3. 重新启动应用


## 卸载指南

### Windows 卸载

#### 方法一：通过控制面板

1. 打开"控制面板"
2. 进入"程序" > "程序和功能"
3. 找到"Antigravity Agent"
4. 右键点击，选择"卸载"
5. 按照卸载向导完成卸载

#### 方法二：通过设置应用

1. 打开"设置"（Win + I）
2. 进入"应用" > "应用和功能"
3. 搜索"Antigravity Agent"
4. 点击应用，选择"卸载"
5. 确认卸载

#### 清理残留文件（可选）

卸载后，配置文件和数据不会自动删除。如需完全清理：

```powershell
# 删除配置目录
rmdir /s %APPDATA%\.antigravity-agent

# 删除开始菜单快捷方式（如果有）
del "%APPDATA%\Microsoft\Windows\Start Menu\Programs\Antigravity Agent.lnk"

# 删除桌面快捷方式（如果有）
del "%USERPROFILE%\Desktop\Antigravity Agent.lnk"
```

### macOS 卸载

#### 卸载应用程序

1. 打开 Finder
2. 进入"应用程序"文件夹
3. 找到"Antigravity Agent"
4. 将其拖到废纸篓
5. 清空废纸篓

或使用命令行：

```bash
# 删除应用程序
sudo rm -rf /Applications/Antigravity\ Agent.app
```

#### 清理残留文件（可选）

```bash
# 删除配置目录
rm -rf ~/.config/.antigravity-agent

# 删除应用支持文件（如果有）
rm -rf ~/Library/Application\ Support/com.antigravity-agent.app

# 删除缓存文件（如果有）
rm -rf ~/Library/Caches/com.antigravity-agent.app

# 删除偏好设置（如果有）
rm -rf ~/Library/Preferences/com.antigravity-agent.app.plist

# 删除日志文件（如果有）
rm -rf ~/Library/Logs/Antigravity\ Agent
```

#### 移除登录项

如果设置了开机自启动：

1. 打开"系统偏好设置"（或"系统设置"）
2. 进入"用户与群组"（或"通用" > "登录项"）
3. 选择"Antigravity Agent"
4. 点击"-"按钮移除

### Linux 卸载（预期）

#### AppImage

```bash
# 直接删除 AppImage 文件
rm Antigravity-Agent_X.X.X_amd64.AppImage

# 删除配置文件
rm -rf ~/.config/.antigravity-agent
```

#### DEB 包（Debian/Ubuntu）

```bash
# 卸载应用
sudo apt remove antigravity-agent

# 完全清除（包括配置文件）
sudo apt purge antigravity-agent

# 清理依赖
sudo apt autoremove
```

#### RPM 包（Fedora/RHEL）

```bash
# 卸载应用
sudo dnf remove antigravity-agent

# 或使用 rpm
sudo rpm -e antigravity-agent
```

### 数据备份建议

在卸载前，建议备份重要数据：

1. **账户配置**：使用应用的"导出"功能导出所有账户配置
2. **数据库文件**：手动备份 `antigravity-agent.db`
3. **日志文件**：如需保留日志，备份 `logs/` 目录

备份位置：
- Windows: `%APPDATA%\.antigravity-agent\`
- macOS/Linux: `~/.config/.antigravity-agent/`

## 下一步

安装完成后，您可以：

- 阅读 [快速开始指南](quickstart.md) 了解基本使用方法
- 查看 [完整使用手册](../user-guide/user-guide.md) 了解所有功能
- 参考 [配置说明](../user-guide/configuration.md) 自定义设置

## 获取帮助

如果在安装过程中遇到问题：

1. 查看 [问题排查手册](../advanced/troubleshooting.md)
2. 搜索 [GitHub Issues](https://github.com/MonchiLin/antigravity-agent/issues)
3. 提交新的 [Issue](https://github.com/MonchiLin/antigravity-agent/issues/new)
4. 参与 [讨论区](https://github.com/MonchiLin/antigravity-agent/discussions)

## 相关文档

- [快速开始](quickstart.md)
- [使用手册](../user-guide/user-guide.md)
- [配置说明](../user-guide/configuration.md)
- [问题排查](../advanced/troubleshooting.md)
- [FAQ](../advanced/faq.md)
